<!DOCTYPE html>
<html>
  <head>
    <title>ComApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%# <% <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet"> %> 
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <h1>ComApp</h1>
            <%# <%= link_to image_tag("logo.png", class: :logo), root_path %>
          </div>
          <%# <% if user_signed_in? %> 
          <%# ログインしているときは以下を表示するようにしましょう %>
            <%# <div class="nav__right"> %>
              <%# <%= link_to "ログアウト", "#", method: :delete, class: :nav__logout %> 
              <%# <%= link_to "新規案内投稿へ", "#", class: :nav__btn %> 
            <%# </div> %>
          <%# // ログインしているときは上記を表示するようにしましょう %>
          <%# <% else %> 
          <%# ログインしていないときは以下を表示するようにしましょう %>
            <div class="nav__right">
              <%= link_to "ログイン", "#", class: :nav__btn %>
              <%= link_to "新規登録", "#", class: :nav__btn %>
            </div>
          <%# // ログインしていないときは上記を表示するようにしましょう %>
          <%# <% end %> 
        </div>
      </div>
    </header>
    <%= yield %>
    <footer class="footer">
      <p class="copyright">Copyright ComApp</p>
    </footer>
  </body>

</html>
